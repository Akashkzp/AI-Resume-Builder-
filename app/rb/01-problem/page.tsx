'use client'

import StepPageContent from '../components/StepPageContent'

export default function ProblemPage() {
  return (
    <StepPageContent step={1} title="Problem">
      <p className="text-gray-600">Define the problem statement and scope for the AI Resume Builder. No resume features yet — route rail and gating only.</p>
    </StepPageContent>
  )
}
